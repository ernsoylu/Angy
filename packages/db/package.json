{
  "name": "@angy/db",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist",
    "prisma",
    "with-env.sh"
  ],
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "./with-env.sh prisma generate --sql && tsc -p tsconfig.build.json",
    "generate": "./with-env.sh prisma generate --sql",
    "migrate": "./with-env.sh prisma migrate dev",
    "seed": "node --env-file-if-exists=../../.env.local --import tsx src/seed.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "migrate:deploy": "./with-env.sh prisma migrate deploy"
  },
  "dependencies": {
    "@angy/shared": "workspace:*",
    "@prisma/client": "^6",
    "bullmq": "^5.81.3",
    "ioredis": "^5.11.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "prisma": "^6",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^3.2.7"
  }
}
