# deps
node_modules/

# builds
dist/
.next/
next-env.d.ts
*.tsbuildinfo

# env & secrets
.env
.env.local
.env.*.local

# test output
coverage/
playwright-report/
# Playwright MCP writes page snapshots here — they capture whatever host was
# being driven, so they must never be committed from a real deployment.
.playwright-mcp/
test-results/

# misc
.DS_Store
*.log
